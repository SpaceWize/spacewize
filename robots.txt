# At the domain root now that the site has its own name, so crawlers
# that only honour a root robots.txt actually read this one.
User-agent: *
Allow: /
Disallow: /notify.html

Sitemap: https://spacewize.dev/sitemap.xml
